/**
 * This is where the user arrives after signing up
 */

import { BlitzPage } from "next"

const CreateProfile: BlitzPage = () => {
  return <h1>Create Profile</h1>
}

export default CreateProfile
